#database structure
#options:
#   none(if table does not exist is gonna fail
#   update: change the db accourding to given structures and entities
#   create: create but does not drop db
#   create-drop: when boot the app create de db but when turn off drop the table
spring.jpa.hibernate.ddl-auto=update

#driver class name (shows witch database to use and how to use it)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.url=jdbc:mysql://localhost:3306/geekylikes
spring.datasource.username=johnwick
spring.datasource.password=savethedog